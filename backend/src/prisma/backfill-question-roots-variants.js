/**
 * OBSOLETE — bank_questions was dropped after the roots/variants cutover.
 * Kept only so old docs/commands fail clearly.
 */
console.error('backfill:question-bank is obsolete (bank_questions dropped).');
process.exit(1);
