# G3Q Analytics

Participation dashboard for the G3Q quiz platform (state → district → taluka). Data is generated in `src/lib/g3q-data.ts`; the backend is not required.

```bash
npm install
npm run dev    # http://localhost:3002
```
