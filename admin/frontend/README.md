# G3Q Admin (Next.js)

Admin console for the bilingual question bank. Talks to the FastAPI backend at `NEXT_PUBLIC_API_URL`.

```bash
npm install
npm run dev
```

Open http://localhost:3000/login

Requires the API running on port 8000 (see root README).
